; VineSuite PHP configuration overrides
upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 256M
max_execution_time = 120

; OPcache (off for dev, enable in production Dockerfile)
opcache.enable = 0
