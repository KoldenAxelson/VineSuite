# Phase 4 Starter Prompt

> Copy the block below into your first AI session for Phase 4.

---

You're picking up VineSuite, a winery SaaS platform. Phases 1–3 are complete (841+ tests passing).

Before writing any code, read `docs/execution/handoffs/07-kmp-shared-core-handoff.md`. That's your onboarding doc — it points to everything else you need. Also read `docs/README.md` and `docs/WORKFLOW.md` so you understand how the project is structured and how we work.

You're building Phase 4: KMP Shared Core — the Kotlin Multiplatform module that powers both the Cellar App and POS App with offline-first event sync, local SQLite via SQLDelight, and a Ktor API client. 8 sub-tasks, work them in order. **One sub-task at a time.** After each sub-task: write the INFO entry, run `make testsuite`, then stop and check in with me before moving to the next one.
